export type WorkExperience = {
    period: `${number} - ${number}` | `${number} - Present`
    jobTitle: string
    company: string
    logoUrl: string
    badgeColor: string
    previewUrl: string
    achievements: string[]
}
