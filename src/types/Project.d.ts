export type ProjectType = {
    id: string
    title: string
    label: string
    description: string
    techStack: string[]
    order: number
    work: boolean;
    img?: string;
    link?: string;
}
